{
  agents: {
    defaults: {
      workspace: "/workspace",
      model: {
        primary: "openai/gpt-5-mini",
      },
    },
  },
  channels: {
    telegram: {
      enabled: true,
      botToken: "${TELEGRAM_BOT_TOKEN}",
      dmPolicy: "allowlist",
      allowFrom: ["tg:1370319633"],
    },
  },
  gateway: {
    mode: "local",
    bind: "loopback",
    reload: { mode: "off" },
  },
}
