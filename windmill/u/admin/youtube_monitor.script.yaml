summary: Workflow 1.2 — YouTube Channel Monitor
description: >
  Hourly: scans YouTube RSS feeds, retrieves transcripts via RapidAPI,
  summarises with Deepseek deepseek-chat, emails batch digest. Skips if no new
  videos. Feed list from u/admin/youtube_feeds variable. Deduplication via
  Windmill state (rolling window of last 1000 video IDs).
lock: '!inline u/admin/youtube_monitor.script.lock'
has_on_behalf_of: false
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    deepseek_key:
      type: string
      description: Deepseek API key
      default: null
      format: password
      originalType: string
    portfolio_db:
      type: object
      description: ''
      default: {}
      properties: {}
    rapidapi_key:
      type: string
      description: RapidAPI key for YouTube transcript
      default: null
      format: password
      originalType: string
    recipient_email:
      type: string
      description: Email address to send the YouTube digest to
      default: ''
      originalType: string
    smtp_resource:
      type: object
      description: Gmail SMTP credentials
      default: null
      format: resource-smtp
    telegram_bot_token:
      type: string
      description: ''
      default: ''
      originalType: string
    telegram_owner_id:
      type: string
      description: ''
      default: ''
      originalType: string
    wm_token:
      type: string
      description: ''
      default: ''
      originalType: string
    youtube_feeds:
      type: string
      description: JSON array of {channel_name, rss_url} objects
      default: null
      format: password
      originalType: string
  required:
    - smtp_resource
    - deepseek_key
    - rapidapi_key
    - youtube_feeds
tag: ''
